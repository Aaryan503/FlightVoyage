name: flight_booking
description: A flight booking application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  google_sign_in: ^6.1.4
  flutter_riverpod: ^2.4.9
  provider: ^6.1.1
  http: ^1.1.2
  intl: ^0.19.0
  flutter_secure_storage: ^9.0.0
  cloud_firestore: ^4.17.5
  syncfusion_flutter_maps: ^29.2.8
  shared_preferences: ^2.5.3
  image_picker: ^1.1.2
  firebase_storage: ^11.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
